{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-outline-info mt-4 mb-2"><i class="far fa-edit"></i>  {{ button_label|default('Enregister') }}</button>
{{ form_end(form) }}
