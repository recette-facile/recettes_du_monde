<form method="post" action="{{ path('zone_geo_delete', {'id': zone_geo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ zone_geo.id) }}">
    <button class="btn">Supprimez</button>
</form>
