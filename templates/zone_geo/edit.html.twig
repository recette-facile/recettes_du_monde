{% extends 'base.html.twig' %}

{% block title %}Modifier la zone géo{% endblock %}

{% block body %}
<div class="m-4">
    <h1>Modifier la zone géographique</h1>

    {{ include('zone_geo/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    {{ include('zone_geo/_delete_form.html.twig') }}

</div>
    <a href="{{ path('zone_geo_index') }}" class="btn col-3 shadow p-3 mb-5 bg-body rounded rounded-pill">
        <i class="fas fa-angle-double-left"></i>   Retour à la liste des zones
    </a>
{% endblock %}
