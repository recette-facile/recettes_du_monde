{% extends 'base.html.twig' %}

{% block title %}Modifier la recette{% endblock %}

{% block body %}
<div class="m-4">
    <h1>Modifier la recette</h1>

    {{ include('recette/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    {# {{ include('recette_ingredient/_form.html.twig') }} #}
    {{ include('recette/_delete_form.html.twig') }}
</div>
    <a href="{{ path('recette_index') }}" class="btn col-3 shadow p-3 mb-5 bg-body rounded rounded-pill m-4">
        <i class="fas fa-angle-double-left"></i>  Liste des recettes
    </a>
</div>
{% endblock %}
