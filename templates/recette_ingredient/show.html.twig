{% extends 'base.html.twig' %}

{% block title %}Ingrédients de cette recette{% endblock %}

{% block body %}
    <h1>Ingrédients de cette recette</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Ingredient</th>
                <td>{{ recette_ingredient.ingredient.nomIngredient}}</td>
            </tr>
        </tbody>
    </table>

     <a href="{{ path('recette_index') }}">Retour à la liste</a>

    {# <a href="{{ path('recette_ingredient_edit', {'id': recette_ingredient.id}) }}">Mettre à jour</a>

    {{ include('recette_ingredient/_delete_form.html.twig') }} #}
{% endblock %} 
