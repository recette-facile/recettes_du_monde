<form method="post" action="{{ path('recette_ingredient_delete', {'recette_ingredient': recette_ingredient.id ,'recette':recette.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recette_ingredient.id) }}">
    <button class="btn">Supprimer</button>
</form>
