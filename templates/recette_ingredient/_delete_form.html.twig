<form method="post" action="{{ path('recette_ingredient_delete', {'recette_ingredient': recette_ingredient.id ,'recette':recette.id}) }}" 
onsubmit="return confirm('Etes-vous sur de vouloir supprimer cet élément?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recette_ingredient.id) }}">
	<button class="btn btn-danger">
		<i class="far fa-trash-alt"></i>   Supprimer</button>
</form>
