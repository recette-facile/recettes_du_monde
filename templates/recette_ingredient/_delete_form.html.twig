<form method="post" action="{{ path('recette_ingredient_delete', {'id': recette_ingredient.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recette_ingredient.id) }}">
    <button class="btn">Supprimer</button>
</form>
