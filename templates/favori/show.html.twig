  
{% extends 'base.html.twig' %}

{% block title %}Favori{% endblock %}

{% block body %}
    <h1>Favori</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id : </th>
                <td>{{ favori.id }}</td>

                <th>Recette : </th>
                <td> 
                    {{ favori.recette.nomRecette }}
                </td>
            </tr>

                <th>Recette : </th>
                <td> 
                    {{ favori.recette.nomRecette }}
                </td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('favori_index') }}">Retour à la liste</a>

    <a href="{{ path('favori_edit', {'id': favori.id}) }}">Modifier</a>

    {{ include('favori/_delete_form.html.twig') }}
{% endblock %}