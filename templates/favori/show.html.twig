{% extends 'base.html.twig' %}

{% block title %}Favori{% endblock %}

{% block body %}
    <h1>Favori</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ favori.id }}</td>
            </tr>
            

            <th>Nom recette</th>
            <td>{{ favori.recette.nomRecette}}</td>


        </tbody>
    </table>

    <a href="{{ path('favori_index') }}">back to list</a>

    <a href="{{ path('favori_edit', {'id': favori.id}) }}">edit</a>

    {{ include('favori/_delete_form.html.twig') }}
{% endblock %}
