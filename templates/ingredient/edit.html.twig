{% extends 'base.html.twig' %}

{% block title %}Modifier ingrédient{% endblock %}

{% block body %}
<div class="m-4">
    <h1>Modifier ingrédient</h1>

    {{ include('ingredient/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    {{ include('ingredient/_delete_form.html.twig') }}
</div>
    <a href="{{ path('ingredient_index') }}" class="btn col-3 shadow p-3 mb-5 bg-body rounded rounded-pill">
        <i class="fas fa-angle-double-left"></i>   Liste des ingrédients
    </a>
{% endblock %}
